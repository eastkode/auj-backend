exports.handler = async (event, context) => {
  // TODO: Implement course changes management logic
  return {
    statusCode: 200,
    body: JSON.stringify({ message: "Course changes endpoint" }),
  };
};
